I will build a complete, modern, scalable single-page React application for a sports products retailer in Lagos, Nigeria, following your specific requirements.

## 1. Project Initialization & Setup
- Create a new Vite project with React and TypeScript: `sports-shop-react`.
- Install dependencies:
    - **Core**: `react-router-dom` (v6.4+), `zustand` (state management).
    - **Styling**: `tailwindcss`, `postcss`, `autoprefixer`, `clsx`, `tailwind-merge`.
    - **Icons**: `lucide-react`.
    - **Forms**: `react-hook-form`, `zod`, `@hookform/resolvers`.
    - **UI/UX**: `swiper` (carousel), `framer-motion` (animations).
    - **Utils**: `date-fns` (if needed), `emailjs-com` (for contact form).

## 2. Configuration & Structure
- Configure **Tailwind CSS** with the specified color palette (Deep Navy `#0a1f44`, Vibrant Orange `#f97316`) and fonts (Poppins, Bebas Neue/Inter).
- Set up the **folder structure** exactly as requested:
    - `src/components/{layout, product, ui, common}`
    - `src/pages`
    - `src/data`
    - `src/store`
    - `src/hooks`, `src/utils`

## 3. Data & State Management
- Create `src/data/products.ts` with 30+ realistic sports items (ID, name, category, price in ₦, image, description, inStock).
- Implement **Zustand store** (`src/store/cartStore.ts`) to manage:
    - Cart items (add, remove, update quantity).
    - Favorites list.
    - Persist state to `localStorage`.

## 4. Component Implementation
- **UI Components**: `Button`, `SearchInput`, `FilterButtons`, `CartDrawer` (responsive).
- **Product Components**: `ProductCard` (with quick view & favorite), `ProductGrid`, `QuickViewModal`.
- **Layout Components**: `Header` (sticky, with cart badge), `Footer`, `Navbar`.
- **Common Components**: `Hero` (with CTA), `CategoryCard`, `TestimonialSlider` (using Swiper).

## 5. Page Development
- **Home**: Hero section, Featured Products carousel, Categories grid, Why Us, Testimonials.
- **Products**: Search bar, Category filters (Football, Basketball, etc.), Product Grid with infinite scroll/load more.
- **Product Details**: (via Modal or dedicated page if needed, focusing on Quick View as requested).
- **Cart**: Dedicated page or drawer showing items, subtotal, and "Checkout via WhatsApp" button.
- **Contact**: Functional form using EmailJS (with placeholder keys) and Google Maps embed/location details.
- **About**: Brand story and values.

## 6. Routing & Final Polish
- Configure `react-router-dom` in `App.tsx` with layout wrapping.
- Implement **ScrollToTop** behavior on route change.
- Ensure **Responsive Design** (mobile-first).
- Add **SEO meta tags** (basic) and ensure accessibility (aria-labels).
- Verify no console errors and optimize performance (lazy loading).

I will start by initializing the project and setting up the structure.